<ul>
  <% if @issues.any? -%>
    <% @issues.each do |issue| -%>
      <%= content_tag(:li, h("#{issue.tracker} ##{issue.issue_id}: #{issue.subject}"), :id => issue.to_param) %>
    <% end -%>
  <% else -%>
    <%= content_tag(:li, l(:label_none), :style => 'display: none;') %>
  <% end -%>
</ul>
